# /build-fix

Detecta errores de TypeScript/compilación, los corrige uno a uno verificando que cada fix no rompa nada más.

Ejecuta build, analiza errores, corrige incrementalmente, verifica tras cada fix.
